[build]
    publish = "www"
    command = "swift run -c release swiftysites --clean"

[build.environment]
    SWIFT_VERSION = "5.9"

[[headers]]
    for = "/*"
        [headers.values]
        Access-Control-Allow-Origin = "*"
